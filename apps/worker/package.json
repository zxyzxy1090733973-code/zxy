{
  "name": "@concept-to-model/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "types": "./dist/main.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node dist/main.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No tests yet for @concept-to-model/worker\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@concept-to-model/application": "workspace:*",
    "@concept-to-model/contracts": "workspace:*",
    "bullmq": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}
